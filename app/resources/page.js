import ResourcesClient from './ResourcesClient';

export const metadata = {
  title: 'Resources — Clas Mortgage Processing',
  description: 'Partner tools and resources for mortgage brokers and loan officers working with Clas Mortgage Processing.',
};

export default function ResourcesPage() {
  return <ResourcesClient />;
}
